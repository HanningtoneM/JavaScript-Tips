let x = 3.14
let y = 5;
let z = 9;
let maximum;
let minimum;

x =Math.round(x); //round both up and down depending on the value
x = Math.floor(x); // round down
x = Math.ceil(x); // round up
x = Math.pow(x, 2); // asssign a power 
x = Math.sqrt(x); // squareroot
x = Math.abs(x); //  to find absolute value(distance away from zero)
maximum = Math.max(x, y, z); // find maxium value
minimum = Math.min(x, y, z); // find minimum